# Frog Trading Exchange Claude Compatibility

Use the repo-root `AGENTS.md` as the authoritative project guide for this workspace.

Local state surfaces:

- `CHANGELOG.md` records project changes and deploy-relevant milestones.
- `.codex/memories.md` records the compact current state for future agents.
- `.codex/sys_arch.md` records the current architecture.

Do not duplicate secrets or long-lived rules here. Keep this file as a compatibility pointer so Claude Code loads the same project context without drifting from `AGENTS.md`.
