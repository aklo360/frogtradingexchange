# Changelog

## Unreleased

### Changed
- Tightened the Robinhood volume scanner's asset policy: canonical USDG (`0x5fc5...d168`) and wrapped WETH (`0x0bd7...ad73`) are removed from discovery, rankings, runner selection, retained trades/pools, both stored signal families, and the authenticated read projection so a retained last-good pre-policy snapshot is sanitized even while market-data refreshes are rate-limited; the GeckoTerminal native-token sentinel (`0x0000...0000`) is normalized to Ethereum/ETH, including legacy retained state. No USDC contract was allowlisted because Robinhood's canonical contract registry does not list USDC and the current explorer results are unrelated user-deployed MIME/launcher tokens rather than issuer-verified Circle USDC. Added regression coverage for filtering, retained-state projection, and native-ETH normalization; the Worker suite passes 211/211.
- Extended and deployed the read-only Robinhood scanner with a bounded volume/new-pair feed. One serialized refresh now unions GeckoTerminal page-one top, trending, and new pools, ranks up to 25 qualifying volume leaders, and emits deduplicated `new_pair`, `high_volume`, or `volume_surge` events using configurable USD, age, ratio, delta, and cooldown thresholds. Production Worker version `23602e96-af77-4f7f-a8fb-314bd2e08c84` runs FTX commit `3a48a23` with only the approved scanner override on; every trading/execution gate remains false. The first volume-capable snapshot at `2026-07-21T05:25:53.444Z` sampled 30 pools, stored 25 leaders and 17 baseline signals, and explicitly labeled a bounded new-pool discovery 429. Verified with 209/209 Worker tests, a successful Wrangler bundle, and the authenticated live snapshot.
- Deployed the approved Robinhood Chain scanner to production `frogx-api` through commits `305f0ad`, `c73cb7e`, and `0bc1398`; final Worker version `be3fc991-1f14-40f6-983b-1db7adc6e52a` runs on the existing five-minute cron with only `ROBINHOOD_ALPHA_SCANNER_ENABLED` overridden on. The first successful persisted snapshot at `2026-07-21T03:55:53.365Z` was provisional with 12 runners, 367 trades, 134 candidate wallets, 0 roster wallets, and 0 signals; ten later pool feeds were explicitly skipped after bounded HTTP 429 retries. All trading/execution gates remained unchanged and false, and no wallet, signing, broadcast, transaction, or Telegram operation occurred in the Worker deployment.
- Hardened the Robinhood Chain scanner after the first production crons exposed GeckoTerminal HTTP 429 throttling, Cloudflare's concurrent-subrequest response limit, and the Workers runtime's strict global-`fetch` receiver requirement. Pool discovery and trade ingestion are now serialized at a 2.5-second public-API cadence, retry 429/5xx responses with bounded `Retry-After`/exponential backoff, cancel rejected response bodies, call injected/global fetch without an object receiver, preserve partial successful pool reads with explicit warnings, and fail closed when every selected trade feed is unavailable. Added regression coverage for 429 recovery, serialized discovery, and receiver-safe fetch invocation; the Worker suite now passes 204/204.
- Removed the Tapestry social integration in full after confirming the vendor's API has been TLS-dead on both production and dev hosts for days with no status page, while their team's public activity has pivoted to their consumer game. Deleted the `/api/tapestry/profiles` edge route, the server client, and the Tapestry type/constant modules; relocated the `NFTAsset` type into the Solana DAS assets service; simplified the Pages worker entry now that no Next-served API routes remain (the UI build is fully static again); and dropped `TAPESTRY_API_KEY` from `.env.example` (the unused Pages secret is intentionally left in place). The profile page now renders only FTX-owned data: wallet-derived username, Privy wallet/Telegram identity chips, verified Business Frog holdings, frog-count achievements (1/5/15 held), and the wallet console. Profile-frog selection persists per wallet in browser storage as an interim until a Privy-custom-metadata profile store lands. Verified with 18/18 UI tests, clean TypeScript/ESLint, a production build, and a Playwright design pass.
- Deployed the profile redesign and simplified burn tracker to production: commits `6275402` and `29f7a45` on `main`, Cloudflare Pages deployments `de745df3` and `16eb2fe3` on project `frogx-ui`. Verified `frogtrading.exchange` serves the new profile connect state and the single-bar burn tracker, and that the live `/api/frogx/buyback` feed populates it (19% at deploy time). The `frogx-api` Worker was not redeployed and no trading gates or secrets changed.
- Rebuilt the web Profile page from scratch as an SNES-style social trading "player card" while keeping the existing Privy/Tapestry/NFT data layer and honest state handling. The new layout adds a hero identity card with the animated conic ring treatment, HUD stat tiles (frogs/followers/following/recent trades), a color-coded trade log from Tapestry trade history with USD values and relative timestamps, a token summary rail, follower/following avatar strips, a social activity feed, threshold-based achievements, and a cyan "wallet console" panel for the Privy wallet inventory. All prior guarantees remain: no fabricated metrics, deterministic connect/loading/error/create states, full-address wallet rows, explicit-only wallet creation, and multi-wallet Business Frog aggregation. Added a development-only `/profile?demo=1` fixture mode (compiled out of production) for design review without live Privy/Tapestry sessions. Verified with 25/25 UI tests, clean TypeScript, ESLint, a production Next.js build, and Playwright screenshots at desktop/mobile widths.
- Simplified the home-page buyback tracker to a single progress bar with an in-bar percentage and a fire emoji at the end. Header copy, floor-price/SOL-needed metrics, and the status pill were removed; polling, the burn-ready toast/confetti celebration, and the post-burn reset behavior are unchanged, and the offline state now renders an em dash instead of an error line.

### Workspace
- Migrated active work from legacy `/Users/aklo/dev/ftx` into managed workspace `/Users/aklo/projects/ftx`.
- Added Codex/Claude local context files for the managed workspace model.
- Set production Cloudflare Worker secret names for `AIRDROP_ADMIN_TOKEN` and `AIRDROP_ETH_RPC_URL`; `AIRDROP_ESCROW_PRIVATE_KEY` remains unset pending the escrow signer.
- Created temporary Solana deposit wallet `7p8n64DoGj1kQ2ChT7mXvbztVgjQEgESgrrqExryoNay` for DAEMON airdrop ETH gas bridging; private key stored in `~/.secrets.env:FROGX_DAEMON_GAS_SOL_DEPOSIT_SECRET_20260528180237`.
- Bridged `0.12 SOL` from the temporary deposit wallet to Ethereum mainnet escrow gas via deBridge order `0xe0b7ac08e8b0e4ecc19f79be011d1f4ce54b20b628d52eb2e6ac80f5def2d57a`; Solana creation tx `4TsdedhxEyyPa31RWHEigxERa1gKpmoAUGNTJKtpPBzpRVbK1TMqqLSvo1HA1khasNFGks2AAdxRE1sv2Km6yQFQ`, Ethereum fulfillment tx `0x48132942dcca7f67fafa39db8d06b83c28dfd7f7d929cbe759bbba6a91a9ad19`.
- Consolidated `0.0073 SOL` more into the temporary gas wallet via txs `A1RbohNvDCZFmjdXZYpkfWHV5jrUzSoQt2J5cUru4XJdFB3JpHCQgFyRnQTtEPvYrm1sCPc9hm4Wvqoq7d7B1PQ` and `2bLrmYrqov7XySCr6t2nKrWphTkhGHadik9wpQPKDGUWahiGV4e7DhiGW2EPKWexGY9YMcrJRVLqSq4v5fw7rxPC`; deBridge still rejected the usable amount as below current economic minimum after transaction rent/fee headroom.
- Created new controllable Ethereum airdrop escrow `0x2c475831b645620A2bE61f1435c2863242470B71`, funded it with `0.01 ETH`, and set its signer as the production Worker secret `AIRDROP_ESCROW_PRIVATE_KEY`; the new escrow still needs `10 $DAEMON` before payout flags can be enabled.
- Replaced the production `AIRDROP_ESCROW_PRIVATE_KEY` with the original escrow signer after verifying the seed derives `0xC853Fc4dE86fC8868Fa89FC3B207d4592Db19e46`, moved `0.009 ETH` from the new escrow to the original escrow in tx `0xbf2264236ec8677e693507d8bb1eac6e72529fc372afd881c288675c2abf87ad`, and enabled payout/autopayout flags.
- Deployed payout-enabled Worker version `62dd0d2c-86fe-4346-888c-753f86d5f9ad` and sent claim #1 payout of `0.10 $DAEMON` to `0xF39221B3382879B35cE360344406937C21d1ee90` in tx `0x1e1c7607714dc7fa9acc6c1e192cc288375d50319a8d1b7dedaef300694b04d2`.

### Added
- Added a disabled-by-default, read-only Robinhood Chain alpha scanner for Ribbot. The Worker combines bounded GeckoTerminal trending/new-pool discovery with the top 100 observed buyers per runner, maintains a rolling 30-day trade window and profitable-wallet roster in the existing global `TradingBotAccountStore`, applies low-sample/spray/copy-correlation/estimated-PNL filters, and stores a convergence signal only when at least four qualified wallets buy the same fresh token inside the configured window. Authenticated `GET /api/frogx/trading-bot/robinhood-alpha` returns the public snapshot only; scheduling requires `ROBINHOOD_ALPHA_SCANNER_ENABLED=true`, has no wallet or execution path, and reports history under 30 observed days as provisional. Verified with 204/204 Worker tests, successful Wrangler dry-run bundles, and a live read-only in-memory GeckoTerminal smoke scan.
- Shared FTX Solana Business Frog holdings service for web and Ribbot. Public `/api/frogx/nfts` and authenticated `/api/frogx/trading-bot/nfts` hard-filter DAS `searchAssets` to the Business Frog collection, verify current ownership, aggregate up to ten embedded wallets, normalize image metadata, and paginate bounded results. Ribbot derives its embedded-wallet inventory from FTX account storage and ignores client wallet input.
- Independent Profile Business Frog loading from the FTX Worker. Holdings from every embedded Privy wallet, loading/error/empty states, and pagination render even when Tapestry social profile sync is unavailable; primary-wallet frogs can still be selected as the profile image when a social profile exists.
- FTX-owned managed-wallet inventory and active-wallet selection foundation. Account storage now maintains up to ten backward-compatible wallet slots, migrates the legacy single-wallet fields into slot 1, persists an active wallet ID, and projects that slot back into the existing address/Privy fields used by direct trading. Privy synchronization accepts only embedded `walletClientType=privy` Solana wallets with wallet IDs, excluding linked external wallets, and authenticated selection records a non-secret account event.
- FTX-authoritative Instant Auto Buy for pasted Telegram contract addresses. The separate default-off account profile stores buy amount, minimum liquidity, optional maximum market cap, and reuses the account's buy slippage/priority fee. Ribbot performs an initial FTX market-risk review for user feedback, while `/execute` independently reloads the stored profile, requires an exact SOL-to-token amount/fee match, reruns token/quote/liquidity/price-impact/market-cap checks, and only then reaches the existing Privy signing boundary.
- Trojan-compatible PumpFun copytrade exclusion backed by target-transaction evidence. When enabled, FTX rejects copied buys or sells whose parsed Solana transaction invokes Pump's official bonding-curve program; PumpSwap AMM transactions remain eligible because Trojan's filter applies only before graduation. The false-by-default flag persists through storage, staged/paused updates, duplication, legacy-row migration, warnings, and Ribbot snapshots.
- FTX-authoritative copytrade update and duplication endpoints. Updates revalidate a complete strategy and are allowed only from `staged` or `paused`; executing/terminal strategies remain immutable. Changing the target wallet clears the old monitor cursor so the next scan establishes a no-trade baseline, while same-target edits retain the consumed-signature cursor. Duplication happens inside the Durable Object and creates a fresh staged strategy with an empty monitor state.
- FTX-owned managed copytrade strategies with optional tags, fixed or percentage buy sizing, target-buy minimums, separate sell priority fees, copy-sell and duplicate-buy controls, renounced-mint enforcement, minimum/maximum market-cap filters, and token blacklists. Strategies persist in the account Durable Object, and `/api/frogx/trading-bot/copytrade/control` pauses or resumes standing monitors without granting Ribbot execution authority.
- Copytrade live execution now derives percentage buys from the target wallet's observed SOL spend, caps every buy at the configured maximum, applies strategy filters inside FTX before `/execute`, mirrors the target wallet's sell percentage against the user's FTX-reported position, and continues to use deterministic Privy references plus read-only no-resend reconciliation.
- FTX-authoritative Trojan-style Ribbot settings: `simple`/`advanced` interface mode, two-to-four configurable buy and sell presets, separate buy/sell priority fees, and sell protection. Preference validation, account storage, public defaults, and the standalone bot share one contract; simple mode forces `confirmTrades=false` at the FTX normalization/storage boundary.
- Bounded read-only confirmed-fill indexing for Ribbot PNL/activity. `/api/frogx/trading-bot/pnl` checks at most 12 missing swap signatures per request through Solana `getTransaction`, validates the stored wallet and expected mint pair, derives wallet-level input/output asset deltas from owned SOL/SPL pre/post balances while neutralizing network fees and token-account rent, and stores one deterministic non-secret `swap_fill_reconciled` event. Malformed, failed, wallet-mismatched, or ambiguous transactions stay on the explicit execution-metadata fallback; reconciliation never signs, broadcasts, or resends. PNL now reports confirmed/estimated coverage and still labels USD cost as net-SOL/current-price estimation rather than DEX-route or realized/FIFO tax-lot accounting.
- FTX-owned operator manual-review queue and evidence-only reconciliation API. A separate `TRADING_BOT_OPERATOR_TOKEN` protects list, acknowledge, and reconcile routes; cases live in the global `TradingBotAccountStore`, acknowledgement changes no execution state, and reconciliation can close a case only through the existing wallet/chain-validated Privy transaction GET or proven bundle terminal result. Normal terminal reconciliation also closes queued cases and records non-secret acknowledgement/resolution events for Ribbot activity.
- Shared bounded-age manual-review escalation for unresolved Privy execution references. Direct swaps/withdrawals, scheduled orders, bundle items, copytrade, sniper, auto-buy, and auto-sell now persist a configurable review deadline (`TRADING_BOT_MANUAL_REVIEW_AFTER_SECONDS`, default 900), raise a deduplicated non-secret `execution_manual_review_required` account event after that deadline, and expose the deadline/reason to Ribbot without retrying, resending, or fabricating a terminal result.
- Disabled-by-default FTX sniper launch monitoring and execution readiness. The advanced cron now reads Jupiter Tokens V2 recent first-pool launches, records an initial cursor without trading existing pools, filters later launches by configured source/liquidity/market-cap/token-authority rules, deduplicates processed mints, enforces cooldown/max-snipes/max-spend limits, and routes an eligible buy only through the existing FTX market-risk, `/execute`, and Privy boundary when the base, monitor, sniper-live, account opt-in, RPC, wallet, signer, and revocation gates all pass.
- Protected read-only `/api/frogx/trading-bot/sniper/status` reconciliation plus account-level `sniperEnabled` controls. Ribbot can rehydrate an unresolved sniper attempt from FTX/Privy without executing or resending, while every sniper monitor/live flag remains false by default.
- FTX control-wallet `restore` state and `bot_access_restored` account events. Restoring the local execution gate remains secondary to Privy's signer/policy enforcement; every live route still rechecks FTX gates and Privy authorization.
- Protected read-only `/api/frogx/trading-bot/copytrade/status`, `/api/frogx/trading-bot/auto-buy/status`, and `/api/frogx/trading-bot/auto-sell/status` endpoints. Ribbot can request reconciliation for one executing config without waiting for cron; FTX reloads the authoritative config, performs the existing wallet/chain-validated Privy GET lookup after the safety window, and never calls execution or resends a transaction.
- Read-only advanced-monitor reconciliation for copytrade, auto-buy, and auto-sell. Every live trigger now persists a deterministic execution ID/reference before FTX calls Privy; cron status checks validate the stored Privy wallet and Solana chain, resolve confirmed/finalized or terminal provider state, and emit deduplicated swap/activity records without resending.
- Protected read-only `/api/frogx/trading-bot/bundle-buy/status` reconciliation. FTX persists attempted/confirmed basket progress, checks only the deterministic Privy references for attempted items, validates the stored wallet and Solana chain, and resolves pending, executed, or failed state without calling wallet RPC or resending an item.
- Protected read-only `/api/frogx/trading-bot/execute/status` and `/api/frogx/trading-bot/withdrawals/status` endpoints. They authenticate Ribbot, recompute the deterministic Privy reference, validate the stored Privy wallet and Solana chain, and report pending/not-found/confirmed/finalized/terminal-failure state without calling wallet RPC or resending a transaction. Reconciled terminal events use deterministic event IDs so repeated checks do not duplicate activity/PNL history.
- Read-only `/perps` terminal prototype with Imperial market data, Coinbase-backed 15-minute candlestick charts for supported markets, route preview, funding/cost panels, GMGN chart links for mapped Solana assets, and a `PERPS` hamburger menu link.
- Public `/api/frogx/trading-bot/config` endpoint exposing non-secret Ribbot trading defaults, FrogX quote/swap route paths, Solana chain metadata, platform fee status, and staged feature gates.
- FTX-owned `/api/frogx/trading-bot/wallet` endpoint for Ribbot wallet setup. It handles quote-only external wallet links without secrets and, when Worker secrets are configured, looks up/creates Telegram-linked Privy users and Solana wallets behind an optional Ribbot bearer token.
- Protected `/api/frogx/trading-bot/swap` endpoint for Ribbot market-order confirmations. It validates Telegram order payloads, requires the Ribbot bearer token, and delegates to the existing FrogX swap builder without signing or broadcasting.
- Protected `/api/frogx/trading-bot/positions` endpoint for Ribbot wallet positions. It requires the Ribbot bearer token, reads SOL and SPL balances through the configured Solana RPC, and returns parsed token amounts for Telegram position cards and percentage sells.
- Protected `/api/frogx/trading-bot/pnl` endpoint for Ribbot PNL cards. It requires the Ribbot bearer token, account storage, and Solana RPC, then combines FTX execution events, live balances, and Jupiter Price V3 data into estimated PNL summaries.
- Protected `/api/frogx/trading-bot/activity` endpoint for Ribbot account history. It requires the Ribbot bearer token plus account storage and returns recent non-secret account events, summary counts, and read-only warnings without signing, broadcasting, or notifying Telegram users.
- Protected `/api/frogx/trading-bot/token-cleanup/review` endpoint for Ribbot token cleanup. It requires the Ribbot bearer token and Solana RPC, prices SPL balances through Jupiter Price V3, and returns review-only dust/hidden/unpriced cleanup candidates without mutating preferences, signing, or broadcasting.
- Protected `/api/frogx/trading-bot/token-safety` endpoint for Ribbot token risk checks. It requires the Ribbot bearer token and Solana RPC, reads parsed SPL mint authority/freeze authority/supply data plus Jupiter Price V3 pricing, and returns review-only low/medium/high risk flags without mutating preferences, signing, or broadcasting.
- Protected `/api/frogx/trading-bot/market-risk` endpoint for Ribbot pre-trade scans. It requires the Ribbot bearer token and Solana RPC, combines token-safety flags, estimated market cap, SOL pricing, and an optional Titan quote/liquidity probe, and returns review-only risk flags without mutating preferences, signing, or broadcasting.
- Protected `/api/frogx/trading-bot/orders/validate` endpoint for Ribbot staged limit, stop-loss, trailing-stop, and DCA orders. It requires the Ribbot bearer token, normalizes order definitions, and returns validation warnings without storing, scheduling, signing, or broadcasting.
- Protected `/api/frogx/trading-bot/orders` and `/api/frogx/trading-bot/orders/cancel` endpoints for FTX-owned staged order persistence. They require the Ribbot bearer token plus `TRADING_BOT_ACCOUNTS`, store/list/cancel non-secret limit, stop-loss, trailing-stop, and DCA metadata in `TradingBotAccountStore`, and still do not schedule, sign, or broadcast.
- Guarded scheduled-order cron runner for FTX-stored limit, stop-loss, trailing-stop, and DCA orders. `TRADING_BOT_SCHEDULER_ENABLED=true` lets the Worker scan the global staged order registry, evaluate Jupiter Price V3 USD triggers, persist trailing peaks and DCA interval state, and record non-secret trigger events; live execution still requires `TRADING_BOT_SCHEDULER_LIVE_EXECUTION_ENABLED=true` plus the normal FTX live execution and Privy signer gates.
- Scheduled-order execution lifecycle state and account events for `executing`, `executed`, and `failed` limit/stop/trailing/DCA runs, including DCA slice progress and non-secret Privy transaction metadata.
- Read-only scheduled-order reconciliation for interrupted or ambiguous Privy sends. After a configurable race window, FTX looks up the persisted execution reference, validates its wallet and Solana chain, resolves confirmed/finalized sends or terminal Privy failures, leaves pending/not-found/look-up-error attempts `executing`, and never resends from reconciliation.
- Guarded advanced automation monitor runner for FTX-stored copytrade and sniper configs. `TRADING_BOT_ADVANCED_MONITOR_ENABLED=true` plus per-kind flags lets the Worker scan the global config registry, baseline/detect copytrade target-wallet signatures through Solana RPC, and persist non-secret monitor state. Sniper monitoring uses the Jupiter recent first-pool feed only when its per-kind monitor flag and `JUPITER_API_KEY` are configured; live execution remains behind a separate false-by-default sniper gate and the normal FTX/Privy controls.
- Disabled-by-default auto-buy and auto-sell monitor checks for FTX-stored rules. `TRADING_BOT_AUTO_BUY_MONITOR_ENABLED=true` records checked token price state while liquidity monitoring remains unconfigured, and `TRADING_BOT_AUTO_SELL_MONITOR_ENABLED=true` observes configured Jupiter Price V3 trigger crossings and records non-secret dry-run trigger state/events. No auto-buy/auto-sell swap, signing, or broadcast is enabled.
- Disabled-by-default live auto-buy execution from the FTX advanced monitor. `TRADING_BOT_AUTO_BUY_LIVE_EXECUTION_ENABLED=true` can consume triggered FTX-stored auto-buy rules only when the base live-execution gate, account auto-buy opt-in, Solana RPC positions/SOL balance, token-safety and market-risk quote-probe checks, FTX-managed Privy wallet match, no bot-access revocation, and Privy signer credentials all pass; execution routes through the existing Privy `signAndSendTransaction` path and records non-secret automation execution events.
- Disabled-by-default live auto-sell execution from the FTX advanced monitor. `TRADING_BOT_AUTO_SELL_LIVE_EXECUTION_ENABLED=true` can consume triggered FTX-stored auto-sell rules only when the base live-execution gate, account auto-sell opt-in, Solana RPC positions, FTX-managed Privy wallet match, no bot-access revocation, and Privy signer credentials all pass; execution routes through the existing Privy `signAndSendTransaction` path and records non-secret automation execution events.
- Disabled-by-default live copytrade execution from the FTX advanced monitor. `TRADING_BOT_COPYTRADE_LIVE_EXECUTION_ENABLED=true` can consume newly observed FTX-stored copytrade target signatures only when the base live-execution gate is true, the parsed target transaction is a simple SOL/SPL buy or sell, copy-sell is enabled for sells, RPC balance/position checks pass, copied buys pass token-safety/market-risk quote probing, and the normal Privy wallet/signer/revocation checks pass through `/execute`; copytrade configs remain active after an execution.
- Disabled-by-default user-triggered live bundle-buy execution. `TRADING_BOT_BUNDLE_BUY_LIVE_EXECUTION_ENABLED=true` can execute stored bundle-buy baskets only when the base live-execution gate is true, RPC SOL balance covers the basket total, every item passes token-safety/market-risk quote probing, and the normal FTX-managed Privy wallet/signer/revocation checks pass through `/execute`; successful baskets are marked executed and record non-secret execution events.
- Protected `/api/frogx/trading-bot/withdrawals/validate` endpoint for Ribbot staged SOL/SPL withdrawals. It requires the Ribbot bearer token, normalizes destination and amount details, and returns validation warnings without building, signing, or broadcasting transfer transactions.
- Protected `/api/frogx/trading-bot/withdrawals/execute` endpoint for Ribbot withdrawal confirmations. It requires the Ribbot bearer token, `TRADING_BOT_LIVE_EXECUTION_ENABLED=true`, RPC access, a stored FTX-managed Privy wallet match, Privy app credentials, and Privy authorization signer credentials before building a SOL/SPL transfer transaction and submitting Privy Solana `signAndSendTransaction`.
- Protected `/api/frogx/trading-bot/copytrade/validate` and `/api/frogx/trading-bot/sniper/validate` endpoints for Ribbot advanced automation configs. They require the Ribbot bearer token, normalize max-buy and liquidity filter details, and return warnings without starting monitors or executing trades.
- Protected `/api/frogx/trading-bot/copytrade`, `/api/frogx/trading-bot/copytrade/cancel`, `/api/frogx/trading-bot/sniper`, and `/api/frogx/trading-bot/sniper/cancel` endpoints for FTX-owned advanced automation persistence. They require Ribbot auth plus `TRADING_BOT_ACCOUNTS`, store/list/cancel non-secret copytrade and sniper metadata in the global automation registry, and never start monitoring or execution from the storage request itself.
- Protected `/api/frogx/trading-bot/auto-buy/validate`, `/api/frogx/trading-bot/auto-buy`, `/api/frogx/trading-bot/auto-buy/cancel`, `/api/frogx/trading-bot/auto-sell/validate`, `/api/frogx/trading-bot/auto-sell`, and `/api/frogx/trading-bot/auto-sell/cancel` endpoints for FTX-owned auto-buy/auto-sell rule persistence. They store non-secret token, cap, liquidity, sell-percentage, and optional trigger metadata in the global automation registry without starting monitors, building swaps, signing, or broadcasting.
- Auto-buy validation and storage now append FTX-owned market-risk review warnings when Solana RPC is configured, giving staged automation rules token-safety, market-cap, and quote/liquidity-probe context before any future execution path can consume them.
- Protected `/api/frogx/trading-bot/bundle-buy/validate`, `/api/frogx/trading-bot/bundle-buy`, and `/api/frogx/trading-bot/bundle-buy/cancel` endpoints for FTX-owned staged bundle-buy baskets. They store/list/cancel non-secret multi-token buy caps and shared liquidity/market-cap filters without starting monitors, building swaps, signing, or broadcasting.
- Protected `/api/frogx/trading-bot/bundle-buy/execute` endpoint for explicit Ribbot bundle execution requests. It requires Ribbot auth plus the bundle live gate, reloads the FTX-stored basket/account, preflights SOL balance and every token's market-risk quote probe, then submits each item through the existing Privy-backed `/execute` boundary.
- Protected `/api/frogx/trading-bot/referrals` endpoints for Ribbot referral/reward hooks. FTX now issues non-secret referral codes, records one referrer relationship per Telegram user, counts referred users, and reports tracking-only rewards without creating fee share, token payout, claimable balance, signing, or transfer state.
- Protected `/api/frogx/trading-bot/preferences/validate` endpoint for Ribbot settings, watchlist, and hidden-token changes. It requires the Ribbot bearer token, normalizes safe preference payloads, and persists non-secret preferences when `TradingBotAccountStore` is bound.
- `TradingBotAccountStore` Durable Object plus authenticated `/api/frogx/trading-bot/account` read endpoint. FTX now stores non-secret Ribbot account snapshots, wallet metadata, settings, watchlists, hidden-token lists, staged automation orders, and account events for recovery/control-page use.
- Short-lived Ribbot account control-code/session endpoints. `/api/frogx/trading-bot/control/code` requires the Ribbot bearer token and stores hashed one-time codes in `TradingBotAccountStore`; the browser consumer is currently removed, so these endpoints are backend-only until a replacement control surface is explicitly approved.
- Session-protected `/api/frogx/trading-bot/control/wallet` actions remain available to an approved future control client. They record non-secret Privy handoff requests without returning keys; Revoke records FTX bot-access revocation, and live swap/withdrawal execution refuses revoked accounts before any Privy call.
- Protected `/api/frogx/trading-bot/execute` endpoint for Ribbot market-order confirmations. It requires the Ribbot bearer token, `TRADING_BOT_LIVE_EXECUTION_ENABLED=true`, a stored FTX-managed Privy wallet match, Privy app credentials, and Privy authorization signer credentials before building a fresh FrogX swap and submitting Privy Solana `signAndSendTransaction`.
- FTX account storage now records non-secret swap and withdrawal execution events after successful Privy sends so future PNL/activity views can be reconstructed server-side.
- DAEMON airdrop claim flow gated to wallets holding at least 1 Solana Business Frog, with a 10+ frog full-prize tier.
- Cloudflare Durable Object coordinator for airdrop claim ordering, duplicate prevention, used Frog mint tracking, and FCFS tiered payout finalization.
- Airdrop API endpoints for config/status, challenge creation, claim submission, admin finalization, and admin export.
- Guarded airdrop payout endpoint and scheduled autopayout path for automatic `$DAEMON` ERC20 sends from the configured escrow signer once production payout switches and chain checks pass.
- Airdrop page matching the home shell with Solana wallet proof, optional Phantom/EVM payout verification, manual Ethereum payout entry, and queued claim status.
- Final `$DAEMON` ERC20 contract and escrow wallet are exposed through airdrop config and admin payout export metadata.
- API unit tests for airdrop config parsing and deterministic tiered payout math.
- Local airdrop eligibility checks can reuse `NEXT_PUBLIC_SOLANA_RPC_URL` as a `SOLANA_RPC_URL` fallback when running the Worker dev harness.
- XP header chip that persists across routes (placeholder `4,269 XP` with sparkle).
- Ribbit profile hub with wardrobe selection, stats, achievements, activity timeline, and quests.
- AudioProvider to keep background music playing between navigations.
- Ribbit XP leaderboard page with glow tiers for top performers.
- Pixel trophy, wallet, swap, and sparkle icons in `/public`.

### Changed
- Removed the unfinished `/perps`, `/leaderboard`, `/airdrop`, and `/ribbot` web pages and all exchange navigation links to them, along with the inactive Help and Chat menu entries. The standalone Ribbot Telegram runtime and FTX trading APIs are unchanged. The connected-wallet placeholder XP chip was removed with the leaderboard surface.
- The Frog buyback-and-burn tracker now renders on the homepage in both `v1` and `v2`, and the Profile page uses the exact homepage background instead of a separate grid treatment.
- Deterministic account-event writes now return the original persisted event after `INSERT OR IGNORE`, so duplicate reconciliation/manual-review writes retain the first authoritative execution timestamp instead of presenting a newer in-memory timestamp.
- Sniper configs now retain non-secret recent-pool cursor, source, liquidity, market-cap, organic-score, processed-mint, execution, and reconciliation metadata. A dry-run observation consumes the launch cursor so enabling live execution later cannot retroactively trade an already observed pool.
- Live copytrade, auto-buy, and auto-sell now atomically claim staged configs before execution. Ambiguous sends remain `executing`; one-shot auto rules resolve to `executed`/`failed`, while standing copytrade configs return to `staged` only after a terminal provider result. Expected status/execution-ID checks reject stale monitor writes.
- Advanced config cancellation conflicts now return the authoritative FTX config snapshot so Ribbot can refresh locked/executed state immediately.
- Bundle execution now atomically claims a staged FTX basket as `executing` before the first item, persists progress around every item, rejects competing claims/cancellation, and uses deterministic per-item execution references. An interrupted partial basket never auto-resumes: reconciliation marks confirmed partial completion `failed`, and remaining items require a fresh basket.
- Direct market and withdrawal execution now returns `pending_reconciliation` with the deterministic Privy reference whenever the send response is ambiguous or lacks a transaction hash. Pre-send withdrawal build failures remain explicitly `not_executable`; callers must use the read-only status endpoint instead of blindly retrying an uncertain Privy request.
- Rebuilt the `/profile` experience around real wallet and Tapestry data. Disconnected, loading, error, no-profile, and populated states now have distinct layouts; connected profiles prioritize the owned Frog collection, profile-frog selection, real social/recent-trade metrics, activity history, and threshold-based milestones in a compact desktop/mobile workspace.
- Live scheduled orders now atomically claim the FTX-stored order before calling the existing `/execute` boundary. Competing cron runs skip claimed orders, DCA slices receive distinct execution IDs, missing live dependencies leave orders staged, and ambiguous sends remain `executing` for read-only Privy reconciliation instead of being replayed or prematurely failed.
- Perps charts now use TradingView Lightweight Charts instead of the hand-drawn SVG candle renderer.
- Background music now defaults to muted, and the perps page includes the shared header mute/unmute button.
- Replaced the shared top-right wallet-shaped navigation trigger with a hamburger icon while keeping the airdrop reachable from the menu.
- Enabled the DAEMON airdrop claim/reservation flow in production config while keeping automatic ERC20 payout flags off until the escrow signer secret is installed.
- Header navigation includes XP readout when connected.
- Leaderboard headers now match pixel SNES styling with neon accents.
- Top 3 leaderboard rows highlight gold/silver/bronze (avatar halo + text glow).

### Fixed
- Profile Business Frog holdings now query every Solana wallet linked to the signed-in Privy account plus the currently connected Solana wallet. Existing Privy wallets are no longer omitted when their linked-account metadata is not classified as embedded by the browser client.
- The Pages API proxy now strips Cloudflare transport headers, retries one thrown upstream subrequest, and returns a bounded JSON `502` if both attempts fail. Profile error parsing rejects HTML documents, preventing intermittent Cloudflare `1101` pages from being dumped into the holdings UI.
- Restored Privy accounts in the FTX web app using the same app ID and Telegram identity as Ribbot. The Profile page now supports Telegram-first recovery of existing embedded Solana wallets, explicit-only wallet creation for new web accounts, wallet inventory/copy/sign-out controls, and an account surface that remains available when optional Tapestry social data is down. No wallet is automatically created or replaced during login.
- Restored Privy wallet readiness against the existing production configuration. The Worker now publishes the known public Privy app ID and accepts the deployed `FROGX_BOT_API_TOKEN` and `PRIVY_SIGNER_ID` aliases while retaining the canonical `RIBBOT_TRADING_BOT_TOKEN` and `PRIVY_AUTHORIZATION_KEY_ID` names. No Privy secret, wallet, signer, policy, or live-execution gate was changed.
- Corrected the Tapestry production base URL from the obsolete `/api/v1` path to the documented `/v1` path, with a regression test covering wallet-profile lookup.
- Restored the Pages-owned Tapestry profile API instead of proxying it to the FTX API Worker. Cloudflare-safe response construction, resilient optional profile enrichment, and visible creation errors allow the profile creation flow to complete or report the provider failure instead of appearing to do nothing.
- FTX no longer reports missing or unavailable Titan quote probing as a generic automation status. Instant Auto Buy, copytrade buys, bundle buys, sniper buys, and token-specific auto-buy now state that liquidity and price impact were not verified and that the result is not a safety pass; execution remains blocked before Privy.
- Advanced auto-buy/auto-sell responses can no longer remain staged and replay on a later cron after FTX loses the Privy response. Competing monitor runs cannot send the same claimed config, and reconciliation confirmation now contributes the canonical deterministic `swap_executed` event used by activity and estimated PNL.
- Bundle status checks no longer race a still-active execution request; fresh sequences stay pending until FTX has recorded reconciliation readiness or the configurable safety window expires.
- Ambiguous bundle item responses can no longer leave the basket staged and replayable. FTX retains `executing` state for read-only reconciliation, while terminal item failures and confirmed partial baskets become `failed` with deterministic aggregate events.
- Direct Privy execution failures no longer produce a blanket response that can be misread as proof that nothing was broadcast. FTX now distinguishes pre-send rejection from unresolved post-request state, and status reconciliation fails closed on wallet/chain mismatches.
- The profile page no longer presents wallet-value-derived "points," fabricated leaderboard rank, always-earned badges, or placeholder social metrics as real user state. Its responsive layout now stays within the viewport at 1440px and 390px browser checks.
- Privy Solana `signAndSendTransaction` requests now include the signed `privy-idempotency-key` header derived from FTX's transaction reference ID, preventing duplicate market, withdrawal, and automation sends during retries within Privy's idempotency window.
- Scheduled-order finalization and reconciliation now use expected status/execution-ID checks, so a stale cron result cannot overwrite a newer execution attempt or emit a false reconciliation event.
- Airdrop UI now distinguishes reserved claims from completed ERC20 payouts: claim status says “Reserved, not sent” until a payout transaction hash exists, the pool counter is labeled as unreserved supply, and paid claims link to Etherscan.
- Airdrop claims now reserve deterministic FCFS `$DAEMON` amounts immediately at claim time, so successful claims show the amount instead of remaining in a queued state.
- Airdrop queued state no longer says “Awaiting finalization,” since payout amounts are deterministic FCFS tiers and no VRF/random prize draw remains.
- Swap confirmation now checks confirmed on-chain errors before showing success.
- Swap execution now refreshes the executable quote immediately before signing instead of reusing a stale preview transaction.
- Platform fee routing now verifies the fee token account exists and matches the fee mint before sending it to Titan; invalid fee accounts are skipped so swaps do not fail.
- Platform fee mint selection now prefers stablecoin fee accounts when available before falling back to wSOL.
- Titan WebSocket protocol negotiation now only advertises the documented uncompressed `v1.api.titan.ag` protocol.
- Buyback progress now includes configured fee token accounts instead of only native SOL above reserve.
- Tapestry server config now resolves secrets lazily so Pages builds do not require local secret material.
- Eliminated duplicate background audio playback when navigating.
- Local Worker dev no longer exposes forwarded env values in process command-line args; it writes ignored `apps/api/.dev.vars` instead.
- Airdrop eligibility now only counts DAS assets whose `ownership.owner` exactly matches the connected Solana wallet, excluding burned/compressed assets so delegated or stale collection matches cannot inflate frog counts.
- Airdrop coordinator now keeps an append-only wallet event log for eligibility checks, challenge creation, and queued claims; admin export includes these events for future points analysis.
- Airdrop payout amounts are now deterministic FCFS tiers: 1-9 frogs receive `0.10` `$DAEMON`; 10+ frogs receive `1.00` `$DAEMON`, until the 10 `$DAEMON` pool is exhausted.
